# Python Project Boilerplates

Easy boilerplate creation

## Setup

1. Choose a project name
2. `mkdir <project-name> && cd <project-name>`
3. Initialize:

```bash
pytyped .
```

1. Setup and check

```bash
git init
make install
make check
git add . && git commit -m initial commit
```
